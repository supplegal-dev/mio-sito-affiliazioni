---
interface Props {
  title: string;
}
const { title } = Astro.props;
---

<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{title}</title>
    <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;900&family=Playfair+Display:ital,wght@1,700;1,900&display=swap" rel="stylesheet">
</head>
<body class="bg-white text-[#111111] antialiased">
    <slot />

    <footer class="mt-32 pb-20 border-t border-gray-100 bg-white">
        <div class="max-w-4xl mx-auto px-6 pt-20 text-center">
            
            <h2 class="serif text-4xl mb-6 italic font-bold tracking-tighter text-[#111111]">
                The Essential <span class="text-[var(--pin-red)]">List</span>
            </h2>

            <div class="max-w-2xl mx-auto mb-12 p-8 bg-[var(--pin-bg-light)] rounded-[var(--pin-radius)] border border-gray-50">
                <p class="text-sm text-[#767676]">
                    In qualità di Affiliato Amazon, questo sito riceve un guadagno dagli acquisti idonei.
                </p>
            </div>

            <div class="space-y-2">
                <p class="text-xs text-gray-400 uppercase tracking-widest font-semibold">
                    &copy; {new Date().getFullYear()} The Essential List
                </p>
                <p class="text-xs text-[var(--pin-red)] uppercase tracking-widest font-bold italic">
                    Curated Amazon Finds
                </p>
            </div>
        </div>
    </footer>
</body>
</html>

<style is:global>
    :root {
        --pin-red: #e60023;
        --pin-bg-light: #f7f7f7;
        --pin-radius: 32px;
    }

    body { 
        font-family: 'Outfit', sans-serif;
        background-color: #ffffff;
    }

    .serif { font-family: 'Playfair Display', serif; }

    .no-scrollbar::-webkit-scrollbar { display: none; }
    .no-scrollbar { -ms-overflow-style: none; scrollbar-width: none; }

    article {
        animation: fadeIn 0.8s ease-out forwards;
    }
    @keyframes fadeIn {
        from { opacity: 0; transform: translateY(15px); }
        to { opacity: 1; transform: translateY(0); }
    }
</style>